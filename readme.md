## Usage

This project need db.

- correct `.env` file
- run `php artisan migrate`
- run `php artisan db:seed`
- run command `php artisan get:flightprices` to get prices from given points
- run command `php artisan check:flights` to check,validate,price change for our flights
